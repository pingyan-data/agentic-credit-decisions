# Extends llm-credit-risk requirements.txt
# Install with: pip install -r requirements_agents.txt

# Existing repo deps (xgboost, shap, pandas, scikit-learn, streamlit) — already installed

# Agent orchestration
langgraph==0.2.50
langchain==0.3.0
langchain-anthropic==0.2.4
langchain-community==0.3.0

# RAG over compliance docs
chromadb==0.5.20
sentence-transformers==3.3.1

# Optional: real LLM (offline mock works without these)
anthropic==0.39.0

# Tracing / debugging (highly recommended for demo)
langsmith==0.1.147
